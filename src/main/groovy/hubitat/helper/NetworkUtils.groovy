package hubitat.helper

/**
 * BUILD-TIME STUB of Hubitat's `hubitat.helper.NetworkUtils` helper.
 * See {@link RMUtils} for the full rationale on why these stubs live
 * on `src/main/groovy/` instead of `src/test/groovy/support/stubs/`
 * (SandboxClassLoader doesn't see test-classpath classes; main
 * classpath is on its parent chain).
 *
 * Not deployed to Hubitat — `packageManifest.json` ships only the
 * two top-level `.groovy` files. On a real hub, `NetworkUtils` is
 * the platform-provided class.
 */
class NetworkUtils {
    static Object sendHubitatCommand(Map params) {
        return null
    }
}
