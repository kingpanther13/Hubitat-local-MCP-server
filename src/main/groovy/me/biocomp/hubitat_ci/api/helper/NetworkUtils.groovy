package me.biocomp.hubitat_ci.api.helper

/**
 * Sandbox-target stub for hubitat.helper.NetworkUtils. See
 * {@link RMUtils} for the full rationale on why both a sandbox-target
 * (here) and a test-target (`hubitat.helper.NetworkUtils`) stub are
 * needed.
 */
class NetworkUtils {
    static Object sendHubitatCommand(Map params) {
        return hubitat.helper.NetworkUtils.sendHubitatCommand(params)
    }
}
