{
    "packageName": "MCP Rule Server",
    "author": "kingpanther13",
    "version": "2.2.2",
    "minimumHEVersion": "2.3.0",
    "dateReleased": "2026-06-12",
    "documentationLink": "https://github.com/kingpanther13/Hubitat-local-MCP-server/blob/main/README.md",
    "communityLink": "",
    "releaseNotes": "v2.2.2 - 2026-06-12\n- fix: VRB-first rule guidance; rebuild e2e restore on the HPM importUrl path (#268)\n  - Clearer guidance steering AI clients to the Visual Rules Builder tools for most automations — including if/then/else rules — with Rule Machine reserved for complex logic\n  - removeAction/removeTrigger now recover automatically from Rule Machine's silently dropped first delete click (verified re-click), completing in ~4-7s instead of timing out with an error\n  - hub_create_backup now triggers the backup asynchronously and confirms via status polling — the old in-app download of the multi-MB archive could trip Hubitat's per-app load limiter\n\nv2.2.1 - 2026-06-11\n- feat: per-app JSON reads, direct-alias app resolution, and hub_call_device_swap (#267)\n  - New: swap a device across every app and rule that references it in one operation (`hub_call_device_swap`), with a compatibility check and blast-radius preview\n  - App configuration can now be read in a fast identity-only summary mode\n  - Event history can now be read per app or rule, not just per device or location\n  - Hub variable management no longer needs a slow first-time discovery scan on hubs that hide the Hub Variables app\n\nv2.2.0 - 2026-06-10\n- docs: list string/decimal for runCommand parameter types (#266)\n  - Docs: the RM action-subtype reference now lists `string`/`decimal` (in addition to `number`) for Run Custom Action parameter types.\n- feat: add Visual Rules Builder tools to the rule machine gateway (#265)\n  - New Visual Rules Builder tools: create, read, update, pause, and delete VRB rules directly — the simplest way for an AI to build basic automations (one JSON write, no wizard)\n    - `hub_get_visual_rule` lists VRB rules or reads one rule's full definition\n    - `hub_set_visual_rule` creates or edits a rule (both the classic and the new graph format are supported)\n    - `hub_delete_visual_rule` safely deletes a VRB rule and returns its definition for recovery\n  - Backups now cover Visual Rules: deleting one through the generic delete tool snapshots its full definition, and `hub_restore_backup` can bring the rule back — definition, name, and pause state included\n  - AI clients are now steered to prefer Visual Rules Builder for simple automations, falling back to Rule Machine for complex logic\n  - Creating a Visual Rule through `hub_set_native_app` now returns a clear pointer to the new tool instead of silently failing",
    "apps": [
        {
            "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
            "name": "MCP Rule Server",
            "namespace": "mcp",
            "location": "https://raw.githubusercontent.com/kingpanther13/Hubitat-local-MCP-server/main/hubitat-mcp-server.groovy",
            "required": true,
            "oauth": true,
            "primary": true
        },
        {
            "id": "b2c3d4e5-f6a7-8901-bcde-f23456789012",
            "name": "MCP Rule",
            "namespace": "mcp",
            "location": "https://raw.githubusercontent.com/kingpanther13/Hubitat-local-MCP-server/main/hubitat-mcp-rule.groovy",
            "required": true,
            "oauth": false,
            "primary": false
        }
    ],
    "drivers": [],
    "bundles": [
        {
            "id": "7e3c1a8f-2b94-4d6e-9a51-2095099012ab",
            "name": "MCP Rule Server Libraries",
            "namespace": "mcp",
            "location": "https://raw.githubusercontent.com/kingpanther13/Hubitat-local-MCP-server/main/bundles/mcp-libraries.zip",
            "required": true
        }
    ]
}
